// Minimal config to allow running locally
process {
    executor = 'local'
    cpus = 1
    memory = '1 GB'
}

params {
    reads = 'assets/testdata/test_{R1,R2}.fq.gz'
    reference = 'assets/testdata/ref.fasta'
    gene_bed = 'assets/testdata/target_gene.bed'
}

